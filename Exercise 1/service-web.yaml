apiVersion: v1
kind: Service
metadata:
  name: webservice
  namespace: frontend
spec:
  selector:
    app: webserver
  ports:
  - name: web
    port: 80
    protocol: TCP
    targetPort: 80
#  type: ClusterIP